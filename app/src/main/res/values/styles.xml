<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_weixin_actionbar</item>
        <item name="colorPrimaryDark">@color/color_weixin_actionbar</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!--夜间模式-->
    <style name="AppTheme.Light.Black" parent="AppTheme">
        <item name="android:colorBackground">@color/colorHuiSe2</item>
    </style>
    <!--日间模式-->
    <style name="AppTheme.Light.White" parent="AppTheme">
        <item name="android:colorBackground">@color/nav_back_color_white</item>
    </style>

    <!--  MainActivity ActionBarItem 配置 -->
    <style name="AppTheme.ActionBarItem">
        <item name="android:textColor">@color/nav_back_color_white</item>
        <item name="android:textSize">20dp</item>
    </style>


</resources>
